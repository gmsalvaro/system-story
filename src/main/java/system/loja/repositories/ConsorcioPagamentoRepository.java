package system.loja.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import system.loja.model.ConsorcioPagamento;

public interface ConsorcioPagamentoRepository extends JpaRepository<ConsorcioPagamento, Long> {

}
